{
    "version": 2,
    "builds": [
      {
        "src": "angular.json",
        "use": "@vercel/static-build",
        "config": {
          "command": "node_modules/.bin/ng"
        }
      }
    ],
    "routes": [
      { "src": ".*", "dest": "/index.html" }
    ]
  }
  